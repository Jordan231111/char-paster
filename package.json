{
  "name": "char-paster",
  "version": "1.0.2",
  "private": true,
  "description": "Firefox extension that bypasses websites blocking copy/paste by typing clipboard or custom text into fields one character at a time.",
  "scripts": {
    "start": "web-ext run --firefox=firefox --browser-console",
    "build": "web-ext build --overwrite-dest",
    "sign": "web-ext sign --channel=unlisted",
    "lint": "web-ext lint"
  },
  "devDependencies": {
    "web-ext": "^10.1.0"
  }
}
